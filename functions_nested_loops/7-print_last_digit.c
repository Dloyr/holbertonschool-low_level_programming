#include "main.h"

/**
 * print_last_digit - print the last digit of a number
 *
 * @r: entry to check
 *
 * Return: Always 0
 */

int print_last_digit(int r)
{
	{
	_putchar( r%10);
	_putchar ( '0' + r);
	}

	return (0);
}
