#include "main.h"

/**
 * _abs - print a function that computes the absolute value
 * of an integer
 *
 * @r: variable to check
 *
 * Return: Always 0
 */

int _abs(int r)
{
	if (r > 0)
	{
		_putchar('r');
	}
	else if (r < 0)
	{
		_putchar(45);
		_putchar('r');
	}
	else
	{
		_putchar (48);
	}
	{
		return (0);
	}
}
